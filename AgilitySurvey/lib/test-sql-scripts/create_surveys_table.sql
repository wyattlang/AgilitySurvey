CREATE TABLE SURVEYS (SURVEYID BIGINT NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1), NAME VARCHAR(64) NOT NULL, ENABLED BOOLEAN NOT NULL, CONSTRAINT PK_SURVEYS PRIMARY KEY (SURVEYID))