
CREATE TABLE CHOICES (CHOICEID BIGINT NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1), QUESTIONID BIGINT NOT NULL, CHOICETEXT VARCHAR(255) NOT NULL, WEIGHT INT NOT NULL, CONSTRAINT PK_CHOICES PRIMARY KEY (CHOICEID))