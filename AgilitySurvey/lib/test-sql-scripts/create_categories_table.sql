CREATE TABLE CATEGORIES (CATEGORYID BIGINT NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1), SURVEYID BIGINT NOT NULL, NAME VARCHAR(255) NOT NULL, CONSTRAINT PK_CATEGORIES PRIMARY KEY (CATEGORYID))
